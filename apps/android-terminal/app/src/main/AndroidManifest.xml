<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Vendor payment/service packages used by the terminal drivers. -->
    <queries>
        <package android:name="com.mypos" />
        <package android:name="com.ftpos.apiservice" />
    </queries>

    <application
        android:allowBackup="false"
        android:label="Retaillink F20 Bridge"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <!-- Terminal-driver diagnostics launcher. Secure payment/PIN/card data
             remains inside the vendor-certified service stack. -->
        <activity
            android:name=".MyPosProbeActivity"
            android:exported="true"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="unspecified" />
    </application>
</manifest>
