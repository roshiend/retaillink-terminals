FROM node:22-alpine

RUN corepack enable && corepack prepare pnpm@10.15.0 --activate
WORKDIR /app

COPY . .
RUN pnpm install --frozen-lockfile && pnpm db:generate && chown -R node:node /app

ENV NODE_ENV=production
USER node

CMD ["pnpm", "--filter", "@retaillink/api", "worker"]
